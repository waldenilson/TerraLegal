#hugo brilhante - fortaleza/ce
#instalacao do rest framework

#    Python (2.7, 3.2, 3.3, 3.4, 3.5)
#    Django (1.7+, 1.8, 1.9)

##pip install ...
##add on settings rest_framework

#serialize
##renderizacao dos objetos python -> json, xml
##serialize extends ModelSerializer
##class Meta: model = Table filds = (...) or exclude = (...)
##JSONRenderer().render(serializer.data) = instancia serializer.data dict -> json

###virtualenv -> virtualrap

###sufixos format=json na view
###mixins

####step-0 ao step-7 = testes e aprendizados
###step-8 -> comecar autenticacao e permissoes
####permission_classes
####routers
####filtering,sorting

######
#Idealizar como utilizar versoes django diferente da 1.3.1